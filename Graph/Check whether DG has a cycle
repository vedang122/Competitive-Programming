/////Note mem(visited,false) before calling and also call with intial vertex.
bool iscyclic(int s)
{
    if(visited[s])
    {
        return true;
    }
    visited[s]=true;
    for(auto i: gp[s])
    {
       if(iscyclic(i))
       {
           return true;
       }
    }
    return false;
}
